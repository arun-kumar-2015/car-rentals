
{
  "entities": {
    "Car": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Car",
      "type": "object",
      "description": "Represents a vehicle available for rent in the car rental service.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Car entity."
        },
        "name": {
          "type": "string",
          "description": "The name or model of the car (e.g., Hyundai Creta)."
        },
        "pricePerDay": {
          "type": "number",
          "description": "The rental price of the car per day in the local currency."
        },
        "fuelType": {
          "type": "string",
          "description": "The type of fuel the car uses (e.g., Petrol, Diesel, Electric)."
        },
        "seats": {
          "type": "number",
          "description": "The number of passenger seats available in the car."
        },
        "transmissionType": {
          "type": "string",
          "description": "The type of transmission in the car (e.g., Automatic, Manual)."
        },
        "imageUrl": {
          "type": "string",
          "description": "URL of the primary image for the car.",
          "format": "uri"
        },
        "frontImageUrl": {
          "type": "string",
          "description": "URL of the car viewed from the front.",
          "format": "uri"
        },
        "leftImageUrl": {
          "type": "string",
          "description": "URL of the car viewed from the left side.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "name",
        "pricePerDay",
        "fuelType",
        "seats",
        "transmissionType",
        "imageUrl",
        "frontImageUrl",
        "leftImageUrl"
      ]
    },
    "Booking": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Booking",
      "type": "object",
      "description": "Represents a customer's reservation for a car rental.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Booking entity."
        },
        "customerName": {
          "type": "string",
          "description": "The full name of the customer making the booking."
        },
        "phoneNumber": {
          "type": "string",
          "description": "The contact phone number of the customer."
        },
        "licenseNumber": {
          "type": "string",
          "description": "The driving license number of the customer."
        },
        "carId": {
          "type": "string",
          "description": "Reference to the Car entity that is being booked."
        },
        "pickupDate": {
          "type": "string",
          "description": "The scheduled date and time for picking up the car.",
          "format": "date-time"
        },
        "returnDate": {
          "type": "string",
          "description": "The scheduled date and time for returning the car.",
          "format": "date-time"
        },
        "pickupLocation": {
          "type": "string",
          "description": "The specified location for picking up the car."
        },
        "totalAmount": {
          "type": "number",
          "description": "The total calculated rental price for the booking duration."
        },
        "status": {
          "type": "string",
          "description": "The current status of the booking (e.g., Pending, Confirmed, Completed)."
        },
        "timestamp": {
          "type": "string",
          "description": "The date and time when the booking record was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "customerName",
        "phoneNumber",
        "licenseNumber",
        "carId",
        "pickupDate",
        "returnDate",
        "pickupLocation",
        "totalAmount",
        "status",
        "timestamp"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/cars/{carId}",
        "definition": {
          "entityName": "Car",
          "schema": {
            "$ref": "#/backend/entities/Car"
          },
          "description": "Stores details about each available car.",
          "params": [
            {
              "name": "carId",
              "description": "The unique identifier for a specific car."
            }
          ]
        }
      },
      {
        "path": "/bookings/{bookingId}",
        "definition": {
          "entityName": "Booking",
          "schema": {
            "$ref": "#/backend/entities/Booking"
          },
          "description": "Contains records of car rental bookings.",
          "params": [
            {
              "name": "bookingId",
              "description": "The unique identifier for a specific booking."
            }
          ]
        }
      },
      {
        "path": "/roles_admin/{adminId}",
        "definition": {
          "entityName": "AdminRole",
          "schema": {
            "type": "object",
            "properties": {
              "uid": { "type": "string" }
            }
          },
          "description": "Identifies users with administrator privileges.",
          "params": [
            {
              "name": "adminId",
              "description": "The Firebase Authentication UID of an admin."
            }
          ]
        }
      }
    ]
  }
}
